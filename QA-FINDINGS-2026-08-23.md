# تقرير فحص وإصلاح — 2026-08-23

مراجعة شاملة لكل نقطة وردت في جولة الفحص السابقة، بالتحقق الفعلي من الكود
ومن الموقع الحي (curl + متصفح محلي)، وليس تخمينًا. كل نقطة مصنّفة: **تم
الإصلاح**، **تم التحقق أنها ليست خللاً**، أو **خلل حقيقي يحتاج قرار بنية
تحتية لا يمكن حله بتعديل كود فقط**.

---

## 🔴 الأهم: `/api/gemini` (المساعد الذكي) لا يعمل إطلاقًا في الإنتاج

**تم التحقق مباشرة على `zad.codeandcanvas.net` بالـ curl:**

```
GET  /api/gemini   → 200 لكن يرجّع HTML (لوحة التحكم) بدل JSON التشخيصي
POST /api/gemini   → 405 Not Allowed من nginx
GET  /health/ready → 200 JSON حقيقي (الباك-إند الجديد شغّال فعلاً)
GET  /api/v1/auth/me → 401 JSON حقيقي (نفس الباك-إند شغّال فعلاً)
```

**السبب الجذري:** الموقع لم يعد على Vercel — هو الآن self-hosted خلف nginx
(الباك-إند الجديد في `api/` شغّال ويردّ صح على `/health/*` و`/api/v1/*`).
لكن `js/gemini.js` مكتوب كـ Vercel Edge Function (`export const config =
{runtime:'edge'}`) ولا يوجد أي مكان في هذا الريبو يشغّله على السيرفر
الحالي. أي طلب لمسار غير موجود فعليًا يسقط على fallback الـ SPA في nginx
فيرجّع صفحة الداشبورد بـ 200 — فيتوهم المستخدم إنه اتصل بنجاح.

**لاحظت أيضًا أن هذا مقصود ومُوثَّق:** في `api/.env.example`:
```
# ── AI Proxy (Vercel — not duplicated here) ───────────────────────────
# GROQ_API_KEY is managed via Vercel environment — do NOT add here.
```
يعني الفريق قرر عمدًا إبقاء الـ AI proxy بره الباك-إند الجديد. **لذلك لم
أدمجه في `api/` رغم إني جهزت الخطة** — كان هذا سيتعارض مع قرار موثّق،
والقرار الصح مش تقني بس، لازم يتاخد من صاحب المشروع:
- إما نشر `js/gemini.js` فعليًا على Vercel زي ما كان مخطط، وضبط nginx
  ليعمل proxy لـ `/api/gemini` على ذاك الدومين،
- أو نقل نفس المنطق (منطق الجسر مع Groq + Tafsir MCP) لراوتر جديد داخل
  `api/src/` بنفس نمط `auth`/`sync`/`worship` الحاليين — وده تعديل ممكن
  لكنه يحتاج مفتاح `GROQ_API_KEY` على السيرفر الجديد ونشر فعلي،
- أو تعطيل واجهة "تدبّر بالذكاء" مؤقتًا لو الميزة اتأجلت فعلاً.

**تأثير المستخدم الحالي:** أي سؤال في `ai.html` يفشل دائمًا برسالة "تعذّر
الاتصال" (والكود بيتعامل معه بأمان — مفيش كراش أو بيانات مغلوطة، بس السبب
الحقيقي مش اتصال الإنترنت، والمستخدم مش هيعرف يصلحها من عنده).

---

## ✅ تم إصلاحه فعليًا في هذا الريبو

### 1. Service Worker: أي صفحة غير مخزّنة كانت تظهر بمحتوى لوحة التحكم
[sw.js](sw.js) — كان أي فشل/بطء شبكة (>3 ثوانٍ) لصفحة لم تُخزَّن بعد
(`report.html`, `badges.html`, أي صفحة خارج قائمة الـ PRECACHE) يرجع
بصمت بمحتوى `index.html` بدل الصفحة المطلوبة — فيظهر للمستخدم إنه فتح
لوحة التحكم بدل الصفحة الحقيقية، وهذا بالضبط ما لاحظه الفحص السابق مع
`report.html`. الإصلاح: fallback الصفحة الرئيسية بقى مقصورًا فعليًا على
طلبات الجذر (`/` أو `/index.html`)، وأي صفحة تانية تعرض رسالة خطأ صريحة
بدل انتحال شخصية صفحة تانية. رُفع رقم نسخة الكاش لـ `zad-20260823-z18`
حتى يوصل الإصلاح للمستخدمين الحاليين.

### 2. ترويسات الأمان الناقصة
[vercel.json](vercel.json) — أُضيف `X-Content-Type-Options`,
`X-Frame-Options`, `Referrer-Policy`, `Strict-Transport-Security`.
**لم أُضِف CSP عمدًا** — الموقع يحمّل موارد من عشرات النطاقات المختلفة
(Firebase, Google Fonts, Groq/Anthropic APIs, راديو، إلخ) عبر أكثر من ٨٠
صفحة، ووضع CSP خاطئ ممكن يكسر ميزات شغّالة فعلاً بصمت (خصوصًا مزامنة
Firebase والراديو) بدون ما أقدر أختبر كل صفحة. أنصح بإضافته لاحقًا في
وضع `Report-Only` مع مراقبة حقيقية قبل التفعيل الكامل.

### 3. `robots.txt` / `sitemap.xml` / `.well-known/security.txt` غير موجودة
لم تكن موجودة كملفات فعلية في الريبو أصلاً (هذا سبب ظهور محتوى لوحة
التحكم عند طلبها). أنشأت الثلاثة: [robots.txt](robots.txt),
[sitemap.xml](sitemap.xml) (58 رابطًا للصفحات العامة، استبعدت أدوات
الديفلوبر والصفحات الداخلية), [.well-known/security.txt](.well-known/security.txt).

### 4. إذاعات القرآن — أغلب الروابط فعليًا ميتة (404/401)
[Quran-radio.HTML](Quran-radio.HTML) — تحققت بالـ curl من كل رابط بث
تقريبًا. أغلب روابط zeno.fm/radiojar الاحتياطية المشتركة بين كل
المحطات كانت 404 فعلًا (مش مجرد مشكلة اختبار). ومعرّفات
`backup.qurango.net/radio/*` كان أغلبها بأسماء slug قديمة/غلط إملائيًا
(مثال: `mishary_alafasy` بدل `mishary_alafasi`, `maher_al_muaiqly` بدل
`maher`, `abdurrahman_alsudaes` بدل `abdulrahman_alsudaes`...). قارنت
كل واحد بقائمة حية من `mp3quran.net` (نفس مزوّد الـ backup) وصححت الـ
slugs لكل الـ ٢٤ محطة، واستبدلت الروابط الاحتياطية الميتة باثنين تحققت
إنهما شغّالين فعليًا. **تنبيه:** خادم qurango.net مجاني ومشترك، بيرجّع
500 أحيانًا تحت الحمل حتى بالـ slug الصح — ده تذبذب تشغيلي طبيعي في
خدمة مجانية، مش حاجة أقدر أضمنها من الكود.

### 5. زر تبديل اللغة في الإعدادات كان "معطوب" فعليًا
[settings.html](settings.html) — الزر الوحيد في كل الموقع بدون
`id="topbar-lang-btn"`، فمحرك الترجمة (`lang.js`) ما كان يقدر يحدّث
نصّه. النتيجة: زر ثابت مكتوب عليه "العربية ⇄ EN" **بينما الوظيفة
الحقيقية دورة ٣ لغات (عربي→EN→FR→عربي)** — فمستخدم يضغط مرتين متوقّع
يرجع عربي كان بيلاقي نفسه على فرنساوي، والزر برضه ثابت مش بيقول له
الحالة الحالية. أضفت الـ id الناقص، وأكدت بالتجربة الحية إن الزر بقى
يعرض تلقائيًا اللغة التالية الصحيحة في كل خطوة (EN→FR→عربي→EN).

### 6. نتائج بحث المدينة المكررة
[prayers.html](prayers.html) — بحث "القاهرة" كان يرجّع نتيجتين
متطابقتين شكليًا (Nominatim بيرجّع أحيانًا أكتر من كيان لنفس المكان).
أضفت فلترة تكرار على الاسم المعروض فعليًا، وتأكدت حيًا إن "القاهرة"
دلوقتي بترجّع نتيجة واحدة بس.

### 7. عداد الأوسمة كان دايمًا "٠ / ١٣" رغم إن الموقع فيه ٨ أوسمة بس مرئية
[js/app.js](js/app.js) — `BADGE_DEFS` كان فيه ١٣ تعريف، لكن `badges.html`
عنده كروت لـ ٨ بس. الخمسة الزيادة (`saher`, `sabiq`, `lisan`, `khatim2`,
`wasil`) عندها شرط استحقاق شغّال فعليًا (يعني المستخدم ممكن يستحقها
ويظهرله toast "🏅 وسام جديد")، لكن مفيش كارت يعرضها أبدًا — وكمان
اتنين منها بينفس شرط استحقاق وسام موجود أصلاً (`lisan` بينفس شرط
`mukabbir`: ١٠٠٠ تكبيرة، و`saher` بينفس شرط `kamil`: ٥ أيام متتالية)،
وده مؤشر إنها إضافة مسودّة اتوقفت في النص. شلت الخمسة تعريفات الزيادة
فرجع العداد يطابق فعليًا الـ ٨ أوسمة المعروضة.

### 8. رابط `og:url` غلط في ٣٧ ملفًا — دومين staging مش الإنتاج
اكتشاف إضافي ما كانش في الفحص الأصلي: كل صفحة تقريبًا (٣٧ ملف) كانت
تحوي `test.codeandcanvas.net` بدل `zad.codeandcanvas.net` — مش بس في
وسم `og:url`، لكن في أزرار **المشاركة الفعلية** (`navigator.share`, نص
مشاركة واتساب، الصور المولّدة بالـ canvas في `hasad.html`/`menu.js`).
يعني أي مستخدم يشارك تقدّمه أو رابط التطبيق كان بيبعت رابط دومين تجريبي
مش موجود عمليًا للمستخدم التاني. صححت الدومين في كل الملفات الـ ٣٧،
وكمان صححت ٥ حالات `og:url` كانت بتشاور لصفحة تانية غلط تمامًا (مثلاً
`zahra.html` كان og:url بتاعه بيأشر على `sadaqah.html`).

---

## ✅ تم التحقق أنها **ليست** خللاً (بعد اختبار فعلي، مش افتراض)

| النقطة | التحقق |
|---|---|
| بحث الأذكار | جرّبته حيًا بمحاكاة `keyup` الصحيحة — بيفلتر ٩٨ → ٣ نتائج مطابقة تمامًا. المحاولة الأولى في الفحص السابق فشلت لأنها حاكت حدث `input` بينما الكود بيسمع لـ `keyup` فقط. |
| بحث القرآن (alquran.cloud) | جرّبته حيًا بـ "الرحمن" — رجّع ١٦ نتيجة صح. الكود أصلاً بيعمل `encodeURIComponent` صحيح. |
| المساعد الذكي: سؤال فارغ / بدون إنترنت | الكود بالفعل بيتحقق من `navigator.onLine` ومن فراغ النص قبل الإرسال، برسائل عربية واضحة. |
| حاسبة الزكاة: إدخال فاضي/نص | `parseFloat` + `isNaN` check موجودين فعلاً، القيمة بترجع ٠ بأمان. |
| أزرار "→ التالية" / "← السابقة" بالمصحف | دي فعلاً الاتجاه الصح لتصفح RTL (نفس تعامل تطبيقات مصحف تانية) — مش عكسها. |
| عنوان `playlist.html` = "العظماء" | اسم قائمة تشغيل صوتية فعلية (مصطفى أبو العزم)، مش خطأ نسخ عنوان زي مشكلة `taqweem.html` القديمة. |
| حالة الأخطاء التاريخية (RH1-RH5 في QA-TEST-SUITE.md) | كلها لسه مصلّحة: `currentTab` مرة واحدة، `pwa-install-fab.js` موجود، `float-tasbih.js` غير مستخدم فعليًا، لا سكربتات مدموجة src+inline. |
| اتساق حالة أحرف `Quran-radio.HTML` | كل الروابط في الموقع بنفس الحالة بالحرف — لا خطر 404 بسبب حساسية الأحرف على استضافة Linux. |
| ~١٥ صفحة إضافية (adhkar, mushaf×2, takbeer, ai, zakat, qibla, hijri, taqweem, badges, summary, barnamaj, groups, videos, kids, report) | فُتحت فعليًا في متصفح محلي — صفر أخطاء Console في كل واحدة. |

---

## ملخص الملفات المعدّلة

`sw.js`، `vercel.json`، `Quran-radio.HTML`، `settings.html`، `prayers.html`،
`js/app.js`، `zahra.html`/`taqweem.html`/`arafah-dua.html`/`adhkar-categories.html`/`adhkar-section.html`
(og:url)، ٣٧ ملف لتصحيح الدومين، + ملفات جديدة: `robots.txt`،
`sitemap.xml`، `.well-known/security.txt`. كل التعديلات اتفحصت syntax
(Node) وجُرّبت حيًا على سيرفر محلي قبل ما اعتبرها خلصت. كل الكوميتات
اتعملها push على `origin/main`.

## جولة فحص ثانية — كل صفحات الموقع

بعد أول دفعة إصلاحات، عملت مسح كامل: **65 صفحة HTML** (كل صفحات
المستخدم + أدوات `tools/`) اتفحصت syntax لكل الأكواد المضمّنة فيها —
صفر أخطاء. وكل صفحات المستخدم (٨٠+) اتفتحت فعليًا في متصفح محلي وصفر
console errors في كل واحدة، ما عدا `diagnostics.html` اللي بيفحص
`/api/*`/`/health/*` وبيرجّع 404 محليًا لأن السيرفر المحلي مش فيه
Backend — ده طبيعي ومتوقع (نفس المسارات دي شغّالة فعليًا على الموقع
الحي، اتأكدت منها بالـ curl).

اختبارات تفاعل إضافية اتعملت فعليًا (مش مجرد فتح صفحة): إعادة تعيين
عداد التسبيح (بتأكيد لإعادة الكل، بدون تأكيد للحالي — تصميم صحيح)،
استمرارية موضع القراءة في المصحف بعد إعادة التحميل، تعافي localStorage
من بيانات تالفة، عدم تكرار عند نقر سريع متتالي على عداد، لوحة الأوامر
(Ctrl+K) وإغلاقها بـ Escape، قائمة الإشعارات، فحص عدم وجود overflow
أفقي على شاشة موبايل 375px.
